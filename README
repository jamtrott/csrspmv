Benchmark program for CSR SpMV
===============================

This is a program for benchmarking the performance of sparse
matrix-vector multiplication (SpMV) for matrices in the compressed
sparse row (CSR) format.
